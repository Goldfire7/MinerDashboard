services:
  - type: web
    name: miner-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn miner_monitor:app --bind 0.0.0.0:$PORT
    plan: free
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
